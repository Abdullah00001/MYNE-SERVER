name: Deploy to VPS

on:
  push:
    branches: [ "main", "master" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Create Backend .env
        run: |
          echo "${{ secrets.ENV_BACKEND }}" > backend-server/.env

      - name: Create AI .env
        run: |
          echo "${{ secrets.ENV_AI }}" > ai-server/.env

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.VPS_SSH_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H ${{ secrets.VPS_HOST }} >> ~/.ssh/known_hosts

      - name: Copy files to VPS via rsync
        run: |
          rsync -avz --exclude '.git' --exclude 'node_modules' --exclude 'dist' --exclude '.venv' ./ ${{ secrets.VPS_USERNAME }}@${{ secrets.VPS_HOST }}:/home/${{ secrets.VPS_USERNAME }}/MYNE-SERVER/

      - name: Execute deployment commands
        run: |
          ssh ${{ secrets.VPS_USERNAME }}@${{ secrets.VPS_HOST }} "cd /home/${{ secrets.VPS_USERNAME }}/MYNE-SERVER && docker compose down && docker compose up -d --build"
